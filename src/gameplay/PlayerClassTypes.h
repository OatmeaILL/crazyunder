#pragma once

// ============================================================================
// PlayerClassTypes —— 职业类型定义（独立头文件，避免循环依赖）
// ----------------------------------------------------------------------------
// 此文件仅包含枚举和类型定义，不依赖 PlayerStats 或任何其他 gameplay 模块。
// Player.h 和 ClassSystem.h 均从此文件引入 PlayerClass 枚举。
// ============================================================================

#include <cstdint>

namespace cu {

// ---- 职业枚举 ----
enum class PlayerClass : uint8_t {
    Mage    = 0,  // 法师（远程弹幕）
    Warrior = 1,  // 剑士（近战范围）
    Count   = 2   // 哨兵
};

} // namespace cu
